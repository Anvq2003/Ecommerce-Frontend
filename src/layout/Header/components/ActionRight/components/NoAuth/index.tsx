import { INoAuth } from '@/layout/Header/types';
import * as React from 'react';

export interface INoAuthBoxProps {
  noAuth: INoAuth;
}

export default function NoAuthBox (props: INoAuthBoxProps) {
  return (
    <div>
      
    </div>
  );
}
